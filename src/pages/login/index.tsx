import LoginForm from "../../components/login"
import AuthContainer from "../../containers/auth"

const Login = () => {
  return (
    <AuthContainer>
     <LoginForm />
    </AuthContainer>
  )
}

export default Login
